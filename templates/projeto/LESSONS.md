# {{nome}} — Lições

> Conhecimento que viaja com o repo: erros que custaram caro, padrões que funcionaram, armadilhas do ambiente. Diferente do `decisions/` (por que escolhemos X) e do `CHANGELOG` (o que entrou) — aqui fica **o que aprendemos na marra**. Agentes consultam antes de começar; o Documenter registra ao fim de um ciclo que ensinou algo não-óbvio.
>
> Uma lição por item. Formato: **o que aconteceu → por quê → como evitar/aplicar**. Se a lição já está num ADR, referencie em vez de duplicar.

## Armadilhas (o que quebrou)

- **{{sintoma curto}}.** {{o que aconteceu e por quê}} → {{como evitar da próxima}}.

## Padrões que funcionaram

- **{{padrão}}.** {{quando aplicar e por que vale}}.

## Ambiente / ferramentas

- **{{ferramenta/OS/dispositivo}}.** {{comportamento não-óbvio + o contorno}}.

# {{nome}} — Contexto (linguagem ubíqua)

> O vocabulário do projeto: os termos do domínio que o código, os docs e as conversas usam — com a definição de cada um. Serve pra naming consistente e pra não re-explicar termo a cada sessão/agente. Diferente de `decisions/` (por que escolhemos), `ARCHITECTURE.md` (como funciona) e `LESSONS.md` (o que erramos): aqui é **o que as palavras significam**.
>
> Regra: um termo por linha; a palavra exata que aparece no código em `monospace`. Termo que muda de nome, atualize aqui e no código juntos. Comece pequeno — só o jargão que confunde.

## Termos do domínio

| Termo | Significa | No código |
|---|---|---|
| {{Termo}} | {{definição curta e inequívoca}} | `{{símbolo/classe/tabela}}` |

## Não confundir

- **{{X}} ≠ {{Y}}** — {{a distinção que já causou confusão}}.

## Fora do vocabulário (evite)

- {{termo ambíguo/legado que NÃO deve ser usado, e o que usar no lugar}}.
